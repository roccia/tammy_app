$("#detail_modal").remove();
$("body").append("<%= j render partial: 'detail', locals: { pics: @pics } %>");
$('#detail_modal').on('shown.bs.modal', function () {
    $(this).find('.modal-dialog').css({width:'auto',
        height:'auto',
        'max-height':'100%'});
});

$("#detail_modal").modal();